# ============================================================================
# Bookings API - Environment Configuration Example
# ============================================================================
# Copy this file to .env and update with your actual values
# NEVER commit the .env file to version control
# ============================================================================

# ----------------------------------------------------------------------------
# Server Configuration
# ----------------------------------------------------------------------------
# Port on which the HTTP server will listen
# Default: 8003
SERVER_PORT=8003

# ----------------------------------------------------------------------------
# Database Configuration (MySQL)
# ----------------------------------------------------------------------------
# MySQL connection string in DSN (Data Source Name) format
# Format: username:password@tcp(host:port)/database?charset=utf8mb4&parseTime=True&loc=Local
#
# Parameters explained:
# - charset=utf8mb4: Support full Unicode (including emojis)
# - parseTime=True: Parse DATE and DATETIME to time.Time automatically
# - loc=Local: Use local timezone for time parsing
#
# Development example:
DATABASE_URL=root:password@tcp(localhost:3306)/bookings_db?charset=utf8mb4&parseTime=True&loc=Local

# Docker example (use service name as host):
# DATABASE_URL=root:password@tcp(mysql:3306)/bookings_db?charset=utf8mb4&parseTime=True&loc=Local

# ----------------------------------------------------------------------------
# JWT Configuration
# ----------------------------------------------------------------------------
# Secret key for signing and validating JWT tokens
# IMPORTANT: Must match the JWT_SECRET used by other microservices
# Generate a strong secret: openssl rand -base64 32
# NEVER use the default value in production!
JWT_SECRET=your-secret-key-change-in-production

# ----------------------------------------------------------------------------
# Message Queue Configuration (RabbitMQ)
# ----------------------------------------------------------------------------
# RabbitMQ connection URL in AMQP format
# Format: amqp://username:password@host:port/vhost
#
# Development (local RabbitMQ):
RABBITMQ_URL=amqp://guest:guest@localhost:5672/

# Docker (use service name):
# RABBITMQ_URL=amqp://guest:guest@rabbitmq:5672/

# Production (with credentials):
# RABBITMQ_URL=amqp://admin:secure_password@rabbitmq.example.com:5672/

# ----------------------------------------------------------------------------
# Application Environment
# ----------------------------------------------------------------------------
# Deployment environment: development, staging, production
# Affects logging verbosity and error handling
# - development: Verbose logging, detailed errors
# - production: Structured logging, sanitized errors
ENVIRONMENT=development

# ----------------------------------------------------------------------------
# Usage Instructions
# ----------------------------------------------------------------------------
# 1. Copy this file:
#    cp .env.example .env
#
# 2. Update the values with your actual configuration
#
# 3. Ensure .env is in .gitignore (DO NOT commit sensitive data)
#
# 4. For Docker deployment, use docker-compose environment variables
#    or mount .env file as a volume
# ============================================================================
